# Si el escudo es "corne_left"
if SHIELD_CORNE_LEFT

config ZMK_KEYBOARD_NAME
    default "Corne"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

config ZMK_SPLIT
    default y

endif

# Si el escudo es "corne_right"
if SHIELD_CORNE_RIGHT

    config ZMK_SPLIT
        default y

endif
